apiVersion: apps/v1
kind: Deployment
metadata:
  name: supervisor-api
  namespace: cognitops
  labels:
    app: supervisor-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supervisor-api
  template:
    metadata:
      labels:
        app: supervisor-api
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: cognitops-workload-sa
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: cognitops-kv-secrets
      containers:
        - name: supervisor-api
          image: __ACR_LOGIN_SERVER__/cognitops/supervisor-api:latest
          ports:
            - containerPort: 8005
          envFrom:
            - configMapRef:
                name: cognitops-config
            - secretRef:
                name: cognitops-secrets
          volumeMounts:
            - name: secrets-store
              mountPath: /mnt/secrets
              readOnly: true
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: supervisor-api
  namespace: cognitops
spec:
  selector:
    app: supervisor-api
  ports:
    - port: 8005
      targetPort: 8005
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: supervisor-api-hpa
  namespace: cognitops
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: supervisor-api
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
